terraform {
  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 5.0"
    }
  }
}

provider "google" {
  project = var.project //Ensure the correct project is entered here!!!
  region  = "us-central1"
}